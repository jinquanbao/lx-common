<?xml version="1.0" encoding="UTF-8"?>
<configuration status="OFF">
    <Properties>
        <Property name="pattern">%date{yyyy-MM-dd HH:mm:ss} [%t] [%-5level]%logger[%L] - %X{traceId} -%message%n</Property>
        <Property name="displayName">app</Property>
    </Properties>
    
    <appenders>
        <!--这个输出控制台的配置-->
        <console name="Console" target="SYSTEM_OUT">
            <!--输出日志的格式-->
            <PatternLayout charset="UTF-8" pattern="${pattern}"/>
        </console>
	    
	    <!--文件会打印出所有信息，这个log每次运行程序会自动清空，由append属性决定，这个也挺有用的，适合临时测试用-->
	    <!-- 
	    <File name="courseerr" fileName="${displayName}_error.log" append="false"> 
	        <ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="DENY" /> 
            <PatternLayout> 
               <pattern>${pattern}</pattern> 
            </PatternLayout> 
        </File>
        -->
         
        <!-- 滚动文件 --> 
        <RollingFile name="INFO" fileName="logs/${displayName}.log"
            filePattern="logs/${displayName}_%d{yyyyMMdd}_%i.log"> 
            <PatternLayout> 
                <pattern>${pattern}</pattern> 
            </PatternLayout> 
            <policies>
	            <TimeBasedTriggeringPolicy modulate="true" interval="1"/>
				<SizeBasedTriggeringPolicy size="100 MB"/>
	        </policies>
	        <DefaultRolloverStrategy max="20">
                <Delete basePath="logs" maxDepth="1">
                    <IfFileName glob="*.log"/>
                     <!-- 设置最大保存时间为15天-->
                    <IfLastModified age="15d"/>
                </Delete>
            </DefaultRolloverStrategy>
        </RollingFile>
    </appenders>

    <loggers>

        <logger name="com.laoxin" level="DEBUG" additivity = "false">
            <appender-ref ref="INFO"/>
            <appender-ref ref="Console"/>
        </logger>

        <root level="INFO">
<!--            <appender-ref ref="INFO" />-->
            <appender-ref ref="Console" />
        </root>
    </loggers>
</configuration>
        