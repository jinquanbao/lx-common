<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.laoxin.test.mapper.mq.ConsumerFailRecordMapper">


    <!--处理表 -->
    <sql id="base"> consumer_fail_record </sql>


	<!-- 查询条件 -->
	<sql id="condition_sql">
            <if test="id != null and id > 0"> and id = #{id}</if>
            <if test="msgId != null and msgId != ''"> and msg_id = #{msgId}</if>
            <if test="type != null and type != ''"> and type = #{type}</if>
            <if test="entityId != null and entityId != ''"> and entity_id = #{entityId}</if>
            <if test="entityObj != null and entityObj != ''"> and entity_obj = #{entityObj}</if>
            <if test="errorCode != null and errorCode != ''"> and error_code = #{errorCode}</if>
            <if test="errorMsg != null and errorMsg != ''"> and error_msg = #{errorMsg}</if>
            <if test="deleted != null and deleted > 0"> and deleted = #{deleted}</if>
            <if test="ids != null and ids != '' "> and id in (${ids}) </if>
	</sql>
	
</mapper>
